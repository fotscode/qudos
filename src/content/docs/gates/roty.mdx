---
layout: '@layouts/math.astro'
title: 'Puerta RotY'
---

import { Code } from '@astrojs/starlight/components'

De forma similar a la puerta <a href={`${import.meta.env.BASE_URL}/gates/y-axis`}>Pauli-Y</a> implica una rotación en el eje $y$ de la esfera de Bloch, esta puerta se acompaña con la rotación empleada. Imitar el comportamiento de la puerta Pauli-Y implica aplicar una RotY con $\pi$ radianes de rotación.

Esta puerta permite abstraerse de ángulos fijos, permitiendo una mayor libertad de aplicación.

En forma de matriz se puede observar de la siguiente manera:
```math
RotY(\theta)=
\begin{bmatrix}
cos\left(\frac{\theta}{2}\right)&- sin\left(\frac{\theta}{2}\right)\\
sin\left(\frac{\theta}{2}\right)&cos\left(\frac{\theta}{2}\right)
\end{bmatrix}
```

En qiskit se puede implementar con la funcion `ry`.

<Code
  code={`
from qiskit import QuantumCircuit
import numpy as np
pi = np.pi
qc = QuantumCircuit(1)
qc.ry(pi, 0) # aplica una rotacion de pi al qubit 0 en el eje y
`

}
  lang='py'
  title='Qiskit | Puerta RotY'
/>

## Mas información

- <a href="https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.RYGate" target="_blank">RYGate (IBM) [Inglés]</a>
- <a href="https://www.quantum-inspire.com/kbase/ry-gate/" target="_blank">Ry Gate (Quantum Inspire) [Inglés]</a>
